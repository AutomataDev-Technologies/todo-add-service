# todo-add-service
This service handles the creation of new TODO items. It exposes an API endpoint to accept user input and stores the data securely in the backend database as part of the TODO microservices system
